<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <TextView
        android:id="@+id/item_name_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toStartOf="@id/item_quantity_text_view"
        app:layout_constraintHorizontal_weight="1"
        android:layout_marginEnd="8dp"
        tools:text="Sample Item Name Very Long To Test Wrapping" />

    <TextView
        android:id="@+id/item_quantity_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:layout_marginEnd="8dp"
        app:layout_constraintEnd_toStartOf="@id/item_price_text_view"
        app:layout_constraintTop_toTopOf="@id/item_name_text_view"
        app:layout_constraintBottom_toBottomOf="@id/item_name_text_view"
        tools:text="Qty: 99" />

    <TextView
        android:id="@+id/item_price_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@id/item_name_text_view"
        app:layout_constraintBottom_toBottomOf="@id/item_name_text_view"
        tools:text="$999.99" />

    <TextView
        android:id="@+id/item_note_text_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textSize="14sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/item_name_text_view"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="Optional note about the item goes here and can also be quite long to see how it wraps." />

</androidx.constraintlayout.widget.ConstraintLayout>